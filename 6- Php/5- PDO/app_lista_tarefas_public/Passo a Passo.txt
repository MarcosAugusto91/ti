1- Criar o diretório app_lista_tarefas fora do diretório público;
2- Dentro criar o arquivo tarefa.model.php
3- Criação da classe tarefa:
    class Tarefa{
        private $id, $id_status, $tarefa, $data_cadastro;
    
        public function __get($atributo){
            return $this->$atributo;
        }

        public function __set($atributo, $valor){
            $this->$atributo = $valor;
        }
        
    }
4- Criar a classe tarefaService: tarefa.service.php na pasta cima: colocando os métodos para realizar o CRUD;

5- Criar o arquivo conexão tamém na pasta cima;

6- No script nova tarefa.php no form colocar o método post e apontar ele para o script tarefa.controller.php. Definir também o Name para os campos que serão preenchidos; Dentro da pasta cima criar também o arquivo tarefa_controller.php o de dentro vai chamar o de cima com require.

7- Inserindo registros no banco: No tarefa_controller.php no diretório cima vamos fazer require dos outros scripts que estão na pasta cima, porém como a chamada virá do tarefa_controller.php do diretório publico o caminho do require será relativo a ele e não do cima;
*****

8- Retornar mensagem avisando que o registro foi feito com sucesso;

9- Começar a fazer a listagem das tarefas, trazendo os dados do banco para a aplicação; Primeiro Refatoring;

10- Construir a função recuperar para trazer os dados para página todas as tarefas, o mesmo processo pode ser reproduzido na página index no final filtrando pelo status 'pendente';

11- Criar a edição na parte do front end, no arquivo todas_tarefas.php buscamos o botão de edição e colocamos o onclick nele;

12- Com os dados recebidos em Tarefa controller basta instanciar um novo objeto que fará a conexão com o banco e dentro de tarefa service fazer a function que autualizará no banco de acordo com a Query criada;

13- Para remover basta fazer o mesmo processo que fez para atualizar a tarefa, mudando a query para um delete. O processo é mais simples.

14- Para finalizar basta fazer mais uma vez o mesmo processo agora clicando no último ícone para mudar o status de pendente para realizado.